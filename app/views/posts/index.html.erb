<p id="notice"><%= notice %></p>

<h1>Secrets</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
      <% if user_signed_in? %>
        <td><%= post.user.email %>
        <% end %>
        <td><%= post.body %></td>
        <% if user_signed_in? && current_user.id == post.user_id %>

        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>
